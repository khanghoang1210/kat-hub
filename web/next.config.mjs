/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['via.placeholder.com', 'urevhrzshuvxvvkuduco.supabase.co'],
},
};

export default nextConfig;
